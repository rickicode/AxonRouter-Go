//go:build tray

package main

// defaultTrayMode makes the tray build start the system tray when the binary
// is launched without any arguments (e.g. double-clicked on Windows).
var defaultTrayMode = true
