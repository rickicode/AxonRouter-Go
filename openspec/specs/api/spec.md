# API Specification

> Generated by openlore v1.0.0 on 2026-07-24 01:29

## Purpose

This document specifies the HTTP API exposed by the system.

## Requirements

### Requirement: APIAuthentication

The API SHALL require authentication via: Bearer JWT, Admin, none, Master API Key

#### Scenario: AuthenticatedRequest
- **GIVEN** a request with valid authentication credentials
- **WHEN** the request is processed
- **THEN** the request is authenticated successfully

#### Scenario: UnauthenticatedRequest
- **GIVEN** a request without authentication
- **WHEN** accessing a protected endpoint
- **THEN** the response status is 401 Unauthorized

### Requirement: POSTMessage

The API SHALL support `POST /v1/messages` to handles chat completion requests in anthropic claude format, supporting both streaming and non-streaming responses with model routing, caching, failover, token budget enforcement, and usage tracking.

#### Scenario: StreamingChatCompletion
- **GIVEN** Authenticated user with valid API key and allowed model access
- **WHEN** POST /v1/messages with Anthropic-compatible request body including stream:true
- **THEN** Returns SSE stream with Claude-format message deltas or full message response
- **AND** Request body is parsed and validated
- **AND** Model is checked against allowlist
- **AND** Token budget is verified
- **AND** Cache is checked for exact hits
- **AND** Connection is selected from pool with failover support
- **AND** Streaming responses use SSE format
- **AND** Non-streaming responses return complete JSON
- **AND** Usage is accumulated for billing
- **AND** Request/response may be compressed

#### Scenario: NonStreamingChatCompletion
- **GIVEN** Authenticated user with valid API key and allowed model access
- **WHEN** POST /v1/messages with Anthropic-compatible request body including stream:false or omitted
- **THEN** Returns 200 OK with complete Claude-format message object
- **AND** Request body is parsed and validated
- **AND** Model is checked against allowlist
- **AND** Token budget is verified
- **AND** Cache is checked for exact hits
- **AND** Connection is selected from pool with failover support
- **AND** Full response is collected before returning
- **AND** Usage is accumulated for billing

#### Scenario: RequestValidationFailure
<!-- openlore-test: tags=smoke,regression (auto) -->
- **GIVEN** Any client making a request
- **WHEN** POST /v1/messages with invalid or disallowed parameters
- **THEN** Returns 400 Bad Request with claudeError JSON detailing the failure
- **AND** Request body exceeds size limit
- **AND** Or request contains invalid JSON
- **AND** Or model is not in allowed list
- **AND** Or token budget is exceeded

#### Scenario: FailoverExhaustion
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with valid request
- **WHEN** POST /v1/messages when no healthy connections available after failover attempts
- **THEN** Returns 503 Service Unavailable or builds failover error response with retry guidance
- **AND** All connections in pool are in cooldown
- **AND** Or all connections fail health checks
- **AND** Or provider returns persistent errors

### Requirement: POSTMessage

The API SHALL support `POST /v1/messages/count_tokens` to counts tokens for a given anthropic-format message request using the provider's native token counting endpoint, returning token usage without generating a completion.

#### Scenario: SuccessfulTokenCount
<!-- openlore-test: priority=high tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid API key and allowed model access
- **WHEN** POST /v1/messages/count_tokens with valid message request body
- **THEN** Returns 200 OK with token count in Anthropic-compatible format
- **AND** Request body is parsed and validated
- **AND** Model is checked against allowlist
- **AND** Executor is resolved for the model
- **AND** Connection is obtained from pool
- **AND** Provider's CountTokens is called
- **AND** Result is returned with proper headers

#### Scenario: TokenCountFailure
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Any client making a request
- **WHEN** POST /v1/messages/count_tokens with invalid parameters
- **THEN** Returns 400 Bad Request with claudeError JSON
- **AND** Request body is invalid
- **AND** Or model is not allowed
- **AND** Or executor resolution fails
- **AND** Or no connection available

### Requirement: POSTEmbedding

The API SHALL support `POST /v1/embeddings` to generate vector embeddings for input text using a specified ai model. routes to appropriate provider (openai-compatible), validates model permissions, checks token budgets, and returns embedding vectors.

#### Scenario: SuccessfulEmbeddingGeneration
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid API key and allowed model access
- **WHEN** POST /v1/embeddings with valid model and input text
- **THEN** 200 OK with embedding vectors array
- **AND** Response includes embedding vectors in OpenAI-compatible format
- **AND** Usage metrics are accumulated for the API key

#### Scenario: MissingModelField
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Any request to embeddings endpoint
- **WHEN** POST /v1/embeddings without 'model' in request body
- **THEN** 400 Bad Request with 'model is required' error
- **AND** Error type is 'invalid_request_error'

#### Scenario: ModelNotAllowed
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with restricted model access
- **WHEN** POST /v1/embeddings with model not in allowed list
- **THEN** 403 Forbidden with 'model not allowed for this API key' error
- **AND** Error type is 'invalid_request_error'

#### Scenario: TokenBudgetExceeded
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user with exceeded or invalid token budget
- **WHEN** POST /v1/embeddings when token budget is depleted
- **THEN** 429 or appropriate error status with budget exceeded message
- **AND** Token budget check fails

#### Scenario: ProviderMissingEmbeddingServiceKind
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Provider configuration exists but lacks embedding support
- **WHEN** POST /v1/embeddings with provider not configured for embeddings
- **THEN** 400 Bad Request with 'provider does not support embeddings' error
- **AND** Error type is 'invalid_request_error'

#### Scenario: ModelNotAvailableForEmbeddingsOnProvider
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Provider has modality restrictions configured
- **WHEN** POST /v1/embeddings with model not in provider's embedding modality list
- **THEN** 400 Bad Request with model availability error for specific provider
- **AND** Error type is 'invalid_request_error'

#### Scenario: NoAvailableConnection
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** No healthy connections available for provider
- **WHEN** POST /v1/embeddings when all provider connections are down
- **THEN** 503 Service Unavailable with 'no available connection' error
- **AND** Error type is 'server_error'

#### Scenario: NonOpenAICompatibleProvider
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Resolved executor does not implement EmbeddingsExecutor interface
- **WHEN** POST /v1/embeddings with provider that doesn't support OpenAI-compatible embeddings
- **THEN** 400 Bad Request with 'embeddings only supported for OpenAI-compatible providers' error
- **AND** Error type is 'invalid_request_error'

### Requirement: POSTImage

The API SHALL support `POST /v1/images/generations` to generate images from text prompts using ai model providers. handles request validation, model permission checks, token budget enforcement, provider resolution, and streaming/non-streaming execution through the executor framework.

#### Scenario: SuccessfulImageGeneration
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid API key and sufficient token budget
- **WHEN** POST /v1/images/generations with valid prompt and model
- **THEN** 200 OK with generated image data in response body
- **AND** Request body is parsed and validated
- **AND** Model is checked against allowed models list
- **AND** Token budget is validated for the requesting organization
- **AND** Provider alias is resolved to concrete service
- **AND** Executor is created with appropriate configuration
- **AND** Request is executed with retry logic
- **AND** API usage is accumulated for billing
- **AND** Response is proxied back to client with appropriate headers

#### Scenario: InvalidRequestBody
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request with invalid JSON body or missing required fields
- **WHEN** POST /v1/images/generations with malformed JSON
- **THEN** 400 Bad Request with error details
- **AND** Error response is written with appropriate status code
- **AND** Usage may still be accumulated if applicable

#### Scenario: ModelNotAllowed
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user requesting disallowed model
- **WHEN** POST /v1/images/generations with unauthorized model ID
- **THEN** 403 Forbidden or 400 Bad Request with model access error
- **AND** Request is aborted before reaching executor
- **AND** Error is logged for monitoring

#### Scenario: TokenBudgetExceeded
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Organization with exhausted or insufficient token budget
- **WHEN** POST /v1/images/generations when token budget is depleted
- **THEN** 429 Too Many Requests or 402 Payment Required with budget error
- **AND** Request is aborted with JSON error response
- **AND** No executor is created or invoked

#### Scenario: UpstreamProviderError
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid request but upstream provider failure
- **WHEN** POST /v1/images/generations when provider service fails
- **THEN** 502 Bad Gateway or 503 Service Unavailable with provider error details
- **AND** Retry logic may be attempted depending on error type
- **AND** Upstream error is proxied to client with appropriate wrapping

### Requirement: POSTImage

The API SHALL support `POST /v1/unified (images dispatch)` to handle image generation requests by dispatching to the images handler. converts request to appropriate format for image generation providers.

#### Scenario: ImageGeneration
- **GIVEN** Authenticated user with image generation request
- **WHEN** POST /v1/unified dispatched to Images handler
- **THEN** 200 OK with generated image data or URL in response format matching OpenAI images API
- **AND** Model is allowed for image generation
- **AND** Request contains valid image generation parameters

### Requirement: POSTSTT

The API SHALL support `POST /v1/audio/transcriptions` to handles speech-to-text transcription requests by receiving multipart audio uploads, validating model permissions, checking token budgets, routing to appropriate stt executor with connection pooling and retry logic, and accumulating api usage metrics.

#### Scenario: SuccessfulSTTTranscription
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid JWT, allowed model access, and sufficient token budget
- **WHEN** POST /v1/audio/transcriptions with multipart audio file and model parameter
- **THEN** 200 OK with transcription result from upstream provider
- **AND** Request Content-Type header is parsed for multipart boundary
- **AND** Audio file is extracted from multipart form data
- **AND** Model ID is validated against allowed models from context
- **AND** Token budget is checked for the requesting organization
- **AND** Connection is selected from pool with health/cooldown checks
- **AND** Request is forwarded to upstream STT provider with retry logic
- **AND** API usage is accumulated for billing/metrics
- **AND** Response is streamed back to client with appropriate headers

#### Scenario: ModelNotAllowed
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with valid JWT but requesting disallowed model
- **WHEN** POST /v1/audio/transcriptions with unauthorized model ID
- **THEN** Error response indicating model is not permitted
- **AND** Model ID is checked against allowed models list from context
- **AND** Request is rejected before processing audio data

#### Scenario: TokenBudgetExceeded
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user whose organization has exceeded token budget
- **WHEN** POST /v1/audio/transcriptions when organization quota is depleted
- **THEN** Error response indicating insufficient token budget
- **AND** Organization ID extracted from context
- **AND** Token budget retrieved and compared against usage
- **AND** Request is rejected if budget exceeded

#### Scenario: NoAvailableConnections
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** All STT provider connections are unhealthy or in cooldown
- **WHEN** POST /v1/audio/transcriptions when connection pool has no healthy endpoints
- **THEN** Error response indicating no available upstream connection
- **AND** Connection pool is exhausted or all connections in cooldown
- **AND** Fallback logic attempts older connection selection
- **AND** Request fails if no viable connection found

#### Scenario: MultipartBodyForwarding
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Valid multipart request with audio file attachment
- **WHEN** POST /v1/audio/transcriptions with multipart/form-data content
- **THEN** Audio data successfully forwarded to upstream STT provider
- **AND** Multipart body is read and preserved for forwarding
- **AND** Content-Type with boundary is maintained for upstream request
- **AND** File handles are properly closed after reading

### Requirement: POSTTTS

The API SHALL support `POST /v1/audio/speech` to convert text to speech using a specified model and voice, returning synthesized audio content. validates model permissions, checks token budgets, manages provider connection selection with fallback, and tracks api usage.

#### Scenario: SuccessfulTTSGeneration
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid JWT, allowed model access, and sufficient token budget
- **WHEN** POST /v1/audio/speech with valid TTS request body containing model, input text, and voice
- **THEN** 200 OK with audio/mpeg or requested audio format stream
- **AND** Response includes Content-Type header matching requested audio format
- **AND** X-Request-ID header is set with unique trace identifier
- **AND** API usage is accumulated for billing

#### Scenario: ModelNotAllowed
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user with disallowed model in request
- **WHEN** POST /v1/audio/speech with model not in user's allowed models list
- **THEN** 400 Bad Request with model access denied error
- **AND** Error response follows standard error format with code and message

#### Scenario: TokenBudgetExceeded
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user with insufficient token budget for requested input
- **WHEN** POST /v1/audio/speech with input exceeding configured token budget
- **THEN** 429 Too Many Requests or 400 Bad Request with budget exceeded error
- **AND** Request is aborted before upstream call

#### Scenario: ProviderConnectionFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid request but all provider connections fail
- **WHEN** POST /v1/audio/speech and getConnection exhausts all provider fallbacks
- **THEN** 502 Bad Gateway or 503 Service Unavailable with upstream connection error
- **AND** Error is logged for observability
- **AND** Client receives sanitized error without upstream details

#### Scenario: RequestBodyTooLarge
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request with body exceeding maximum allowed size
- **WHEN** POST /v1/audio/speech with request body larger than configured max bytes
- **THEN** 413 Payload Too Large or 400 Bad Request with body read error
- **AND** Request body size limit is enforced via http.MaxBytesReader

### Requirement: POSTUnifiedRequest

The API SHALL support `POST /v1/unified` to unified endpoint that dispatches to the appropriate handler based on request mode. supports chat completions, images, and video generation modes by inspecting the request body and routing to the corresponding internal handler.

#### Scenario: ChatCompletionsMode
- **GIVEN** Authenticated user with valid JWT token and request within size limits
- **WHEN** POST /v1/unified with mode=chat or inferred chat completion request
- **THEN** Request is dispatched to ChatCompletions handler with streaming or non-streaming response based on
stream flag
- **AND** Request body contains valid mode field
- **AND** Model is allowed for the authenticated user
- **AND** Token budget is sufficient

#### Scenario: ImagesGenerationMode
- **GIVEN** Authenticated user with valid JWT token
- **WHEN** POST /v1/unified with mode=images or inferred image generation request
- **THEN** Request is dispatched to Images handler for image generation
- **AND** Request body contains image generation parameters
- **AND** Model is allowed for the authenticated user

#### Scenario: VideoGenerationMode
- **GIVEN** Authenticated user with valid JWT token
- **WHEN** POST /v1/unified with mode=video or inferred video generation request
- **THEN** Request is dispatched to Video handler for video generation
- **AND** Request body contains video generation parameters
- **AND** Model is allowed for the authenticated user

#### Scenario: RequestBodyTooLarge
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Any request with body larger than configured max bytes
- **WHEN** POST /v1/unified with oversized request body
- **THEN** 413 Payload Too Large with read body error details
- **AND** Request body exceeds maximum size limit

#### Scenario: BudgetOrPermissionDenied
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user with exhausted token budget or unauthorized model
- **WHEN** POST /v1/unified with disallowed model or exceeded budget
- **THEN** 403 Forbidden or appropriate error response with budget/permission details
- **AND** User has exceeded token budget or model is not allowed

### Requirement: POSTChatCompletion

The API SHALL support `POST /v1/unified (chat completions dispatch)` to handle chat completion requests including streaming and non-streaming responses. supports model fusion, combo requests, caching, failover between providers, token usage tracking, and rate limit handling.

#### Scenario: NonStreamingChatCompletion
- **GIVEN** Authenticated user with valid request
- **WHEN** POST /v1/unified dispatched to ChatCompletions with stream=false or no stream flag
- **THEN** 200 OK with JSON completion response including usage statistics, cached or from upstream provider
- **AND** Model is allowed via modelIDAllowed check
- **AND** Token budget sufficient via checkTokenBudget
- **AND** Optional fusion/combo detection for multi-model requests
- **AND** Cache lookup for exact match
- **AND** Provider resolution and proxy candidate selection

#### Scenario: StreamingChatCompletion
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user requesting streaming response
- **WHEN** POST /v1/unified dispatched to ChatCompletions with stream=true
- **THEN** 200 OK with text/event-stream or NDJSON chunked response, with proper cancellation handling and
failover on errors
- **AND** Streaming enabled via stream=true or stream options
- **AND** Model supports streaming
- **AND** Provider connection established

#### Scenario: FusionRequestHandling
- **GIVEN** Authenticated user with complex multi-model requirement
- **WHEN** POST /v1/unified with fusion-capable model configuration
- **THEN** Request processed through handleFusionRequest combining multiple model capabilities
- **AND** Fusion mode detected via combo.DetectRequiredCapabilities
- **AND** Multiple models required for request

#### Scenario: CacheHitServing
- **GIVEN** Authenticated user with previously cached identical request
- **WHEN** POST /v1/unified with exactCacheKey matching stored entry
- **THEN** 200 OK with cached response body, skipping upstream provider call
- **AND** Exact cache key match found
- **AND** Cache entry is fresh

#### Scenario: ProviderFailover
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with failing primary provider
- **WHEN** POST /v1/unified where primary provider returns failover-eligible error
- **THEN** Request retried with next available provider, exponential backoff applied, ban count tracked
- **AND** Primary provider fails with eligible error
- **AND** Failover candidates available
- **AND** Ban count allows retry

### Requirement: POSTVideo

The API SHALL support `POST /v1/unified (video dispatch)` to handle video generation requests by dispatching to the video handler. converts request to appropriate format for video generation providers.

#### Scenario: VideoGeneration
- **GIVEN** Authenticated user with video generation request
- **WHEN** POST /v1/unified dispatched to Video handler
- **THEN** 200 OK with video generation task status or result
- **AND** Model is allowed for video generation
- **AND** Request contains valid video generation parameters

### Requirement: POSTVideoGeneration

The API SHALL support `POST /v1/video/generations` to generate a video from a text prompt or image input using ai models. handles model validation, token budget checking, connection pooling to upstream providers, request proxying with retry logic, and usage accumulation.

#### Scenario: SuccessfulVideoGeneration
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid JWT, allowed model ID, and sufficient token budget
- **WHEN** POST /v1/video/generations is called with valid request body containing model and prompt
- **THEN** 200 OK with generated video content proxied from upstream provider
- **AND** Upstream provider returns 200 with video generation result
- **AND** API key usage is accumulated for billing

#### Scenario: ModelNotAllowed
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with disallowed model ID in request
- **WHEN** POST /v1/video/generations is called with unauthorized model ID
- **THEN** 400 Bad Request with error indicating model is not allowed
- **AND** Error response indicates model not allowed

#### Scenario: TokenBudgetExceeded
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user with exceeded token budget
- **WHEN** POST /v1/video/generations is called when token budget is depleted
- **THEN** 429 Too Many Requests or 403 Forbidden with budget exceeded error
- **AND** Error response indicates insufficient budget

#### Scenario: UpstreamConnectionFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid request but all upstream connections fail
- **WHEN** POST /v1/video/generations is called and no healthy upstream provider is available
- **THEN** 502 Bad Gateway or 503 Service Unavailable with upstream error details
- **AND** Retry logic may attempt fallback connections
- **AND** Error is logged for observability

#### Scenario: RequestBodyTooLarge
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request with excessively large body
- **WHEN** POST /v1/video/generations is called with request body exceeding size limit
- **THEN** 413 Payload Too Large with body read error
- **AND** Request body size is limited by http.MaxBytesReader

### Requirement: POSTResponse

The API SHALL support `POST /v1/responses` to handle openai responses api format requests with support for streaming, model routing, caching, failover, compression, token budgeting, and quota management

#### Scenario: NonStreamingSuccessfulResponse
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid API key and request body in OpenAI Responses format
- **WHEN** POST /v1/responses with stream: false
- **THEN** 200 OK with JSON response body containing output, usage, and id
- **AND** Request body is read and optionally compressed
- **AND** Model is validated against allowlist
- **AND** Token budget is checked
- **AND** Exact cache is checked for hits
- **AND** Capabilities are detected for routing

#### Scenario: StreamingSuccessfulResponse
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid API key and request body in OpenAI Responses format
- **WHEN** POST /v1/responses with stream: true
- **THEN** 200 OK with chunked SSE stream containing response deltas
- **AND** Request body is read and optionally compressed
- **AND** Model is validated against allowlist
- **AND** Token budget is checked
- **AND** Exact cache is checked for hits
- **AND** Streaming executor is resolved and executed

#### Scenario: TokenBudgetExceeded
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Authenticated user with valid API key
- **WHEN** POST /v1/responses with request exceeding configured token budget
- **THEN** 429 Too Many Requests or 400 Bad Request with error about token budget
- **AND** Request body is read and optionally compressed
- **AND** Model is validated against allowlist
- **AND** Token budget is checked

#### Scenario: ModelNotAllowed
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with valid API key
- **WHEN** POST /v1/responses with model not in allowlist
- **THEN** 400 Bad Request with error about disallowed model
- **AND** Request body is read and optionally compressed
- **AND** Model validation fails

#### Scenario: RequestBodyTooLarge
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Any client request
- **WHEN** POST /v1/responses with body exceeding MaxBytesReader limit
- **THEN** 413 Payload Too Large with error message
- **AND** Request body read fails due to size limit

#### Scenario: ExactCacheHit
- **GIVEN** Authenticated user with identical previous request
- **WHEN** POST /v1/responses with matching cache key
- **THEN** 200 OK with cached response body, skipping upstream call
- **AND** Exact cache key matches previous request

#### Scenario: FailoverToSecondaryUpstream
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated user with failing primary upstream
- **WHEN** POST /v1/responses during upstream outage
- **THEN** 200 OK with response from failover upstream, or aggregated error if all fail
- **AND** Primary upstream fails with eligible error
- **AND** Failover is configured

#### Scenario: QuotaCooldownApplied
- **GIVEN** Authenticated user with exhausted quota
- **WHEN** POST /v1/responses for model in cooldown period
- **THEN** Response with cooldown headers, quota exhaustion persisted to cache
- **AND** Quota exhaustion detected for model scope

### Requirement: POSTProxyPool

The API SHALL support `POST /api/proxy-pools/vercel-deploy` to deploy a relay edge function to vercel's edge network, creating a new proxy pool entry with generated authentication and testing the deployed endpoint before saving.

#### Scenario: SuccessfulVercelDeployment
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated admin with valid Vercel deployment credentials and relay configuration
- **WHEN** POST /api/proxy-pools/vercel-deploy is called with deployment parameters
- **THEN** Returns 200 OK with deployment details and saved pool information
- **AND** Polls Vercel deployment status until ready
- **AND** Tests the deployed relay URL for health
- **AND** Saves the new proxy pool to database with active status

#### Scenario: VercelAPIError
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Vercel API returns non-2xx status during deployment creation
- **WHEN** POST /api/proxy-pools/vercel-deploy is called
- **THEN** Returns 500 Internal Server Error with descriptive error message
- **AND** Returns parsed Vercel error message from response body

#### Scenario: VercelDeploymentTimeout
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Vercel deployment creation succeeds but deployment never reaches ready state
- **WHEN** POST /api/proxy-pools/vercel-deploy is called
- **THEN** Returns 500 Internal Server Error with timeout error message
- **AND** Deployment polling exceeds configured timeout duration

#### Scenario: RelayHealthCheckFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Vercel deployment succeeds but deployed relay endpoint fails health check
- **WHEN** POST /api/proxy-pools/vercel-deploy is called
- **THEN** Returns 500 Internal Server Error with relay test failure details
- **AND** Relay health check fails after successful deployment

### Requirement: POSTProxyPool

The API SHALL support `POST /api/proxy-pools/deno-deploy` to deploy a relay edge function to deno deploy's edge network, creating a new proxy pool entry with generated authentication and testing the deployed endpoint before saving.

#### Scenario: SuccessfulDenoDeployment
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated admin with valid Deno Deploy token and relay configuration
- **WHEN** POST /api/proxy-pools/deno-deploy is called with deployment parameters
- **THEN** Returns 200 OK with deployment details and saved pool information
- **AND** Polls Deno deployment revision status until ready
- **AND** Tests the deployed relay URL for health
- **AND** Saves the new proxy pool to database with active status

#### Scenario: DenoAPIError
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Deno Deploy API returns non-2xx status during deployment
- **WHEN** POST /api/proxy-pools/deno-deploy is called
- **THEN** Returns 500 Internal Server Error with descriptive error message
- **AND** Returns parsed Deno error message from response body

#### Scenario: DenoRevisionTimeout
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Deno deployment succeeds but revision never reaches ready state
- **WHEN** POST /api/proxy-pools/deno-deploy is called
- **THEN** Returns 500 Internal Server Error with timeout error message
- **AND** Revision polling exceeds configured timeout duration

#### Scenario: OrganizationSlugNormalization
- **GIVEN** Deno project identifier includes organization prefix or trailing slash
- **WHEN** POST /api/proxy-pools/deno-deploy is called with org-prefixed project
- **THEN** Correctly extracts and formats organization slug for API calls
- **AND** Organization slug is normalized from project identifier

### Requirement: POSTProxyPool

The API SHALL support `POST /api/proxy-pools/cloudflare-deploy` to deploy a relay edge function to cloudflare workers, creating a new proxy pool entry with generated authentication and testing the deployed endpoint before saving.

#### Scenario: SuccessfulCloudflareDeployment
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated admin with valid Cloudflare API credentials and relay configuration
- **WHEN** POST /api/proxy-pools/cloudflare-deploy is called with deployment parameters
- **THEN** Returns 200 OK with deployment details and saved pool information
- **AND** Uploads worker script to Cloudflare
- **AND** Deploys worker to edge
- **AND** Tests the deployed relay URL for health
- **AND** Saves the new proxy pool to database with active status

#### Scenario: CloudflareAPIError
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Cloudflare API returns non-2xx status during script upload or deployment
- **WHEN** POST /api/proxy-pools/cloudflare-deploy is called
- **THEN** Returns 500 Internal Server Error with descriptive error message
- **AND** Returns parsed Cloudflare error message from response body

#### Scenario: CloudflareDeploymentFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Cloudflare script upload succeeds but deployment fails
- **WHEN** POST /api/proxy-pools/cloudflare-deploy is called
- **THEN** Returns 500 Internal Server Error with deployment failure details
- **AND** Deployment status polling indicates failure

### Requirement: POSTProxyPool

The API SHALL support `POST /api/proxy-pools/generate-source` to generate the edge function source code for a specified relay type without deploying, returning the complete runnable edge function code that can be manually deployed.

#### Scenario: GenerateEdgeFunctionSource
- **GIVEN** Authenticated admin with valid relay type specification
- **WHEN** POST /api/proxy-pools/generate-source is called with relay type parameter
- **THEN** Returns 200 OK with generated source code in response body
- **AND** Replaces template variables with generated values
- **AND** Returns complete edge function source as string

#### Scenario: InvalidRelayType
<!-- openlore-test: tags=smoke,regression (auto) -->
- **GIVEN** Invalid or unsupported relay type requested
- **WHEN** POST /api/proxy-pools/generate-source is called with unknown relay type
- **THEN** Returns 400 Bad Request with validation error
- **AND** Relay type must be one of supported edge platforms

### Requirement: GETProxyGroup

The API SHALL support `GET /api/admin/proxy-groups` to list all proxy groups with optional filtering and pagination.

#### Scenario: ListProxyGroups
- **GIVEN** Admin user is authenticated
- **WHEN** GET /api/admin/proxy-groups is called with optional query parameters
- **THEN** Returns a paginated list of proxy groups
- **AND** Supports pagination via query parameters
- **AND** Supports filtering by active status

### Requirement: GETProxyGroup

The API SHALL support `GET /api/admin/proxy-groups/:id` to get a single proxy group by its id.

#### Scenario: GetProxyGroup
- **GIVEN** Admin user is authenticated and proxy group ID exists
- **WHEN** GET /api/admin/proxy-groups/:id is called
- **THEN** Returns the proxy group details
- **AND** Returns 404 if proxy group does not exist

### Requirement: POSTProxyGroup

The API SHALL support `POST /api/admin/proxy-groups` to create a new proxy group with associated proxy pools.

#### Scenario: CreateProxyGroup
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated with valid proxy group data
- **WHEN** POST /api/admin/proxy-groups is called with valid JSON body
- **THEN** Creates and returns the new proxy group
- **AND** Validates required fields
- **AND** Processes pool IDs from request
- **AND** Generates UUID for new group

### Requirement: PUTProxyGroup

The API SHALL support `PUT /api/admin/proxy-groups/:id` to update an existing proxy group's configuration and associated pools.

#### Scenario: UpdateProxyGroup
- **GIVEN** Admin user is authenticated and proxy group ID exists
- **WHEN** PUT /api/admin/proxy-groups/:id is called with valid JSON body
- **THEN** Updates and returns the modified proxy group
- **AND** Validates proxy group exists before update
- **AND** Processes pool IDs from request
- **AND** Updates timestamp on modification

### Requirement: DELETEProxyGroup

The API SHALL support `DELETE /api/admin/proxy-groups/:id` to delete a proxy group and clean up all references to it.

#### Scenario: DeleteProxyGroup
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and proxy group ID exists
- **WHEN** DELETE /api/admin/proxy-groups/:id is called
- **THEN** Deletes the proxy group and returns success
- **AND** Calls cleanGroupReferences to remove all references before deletion
- **AND** Returns 404 if proxy group does not exist

### Requirement: GETMasterKey

The API SHALL support `GET /admin/api/v1/master-key` to returns the current master key, its prefix, the admin base url, and when the key was created.

#### Scenario: RetrieveMasterKeyMetadata
- **GIVEN** Authenticated admin user with valid master key
- **WHEN** GET /admin/api/v1/master-key is called
- **THEN** 200 OK with master key metadata in response body
- **AND** Response body contains key, prefix, base_url, and created_at fields

### Requirement: POSTMasterKey

The API SHALL support `POST /admin/api/v1/master-key/regenerate` to rotates the master key and returns the new metadata.

#### Scenario: RegenerateMasterKeySuccessfully
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated admin user with valid master key
- **WHEN** POST /admin/api/v1/master-key/regenerate is called
- **THEN** 200 OK with new master key metadata in response body
- **AND** New master key is persisted
- **AND** Response contains new key, prefix, base_url, and created_at

#### Scenario: RegenerateMasterKeyFails
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated admin user
- **WHEN** POST /admin/api/v1/master-key/regenerate is called and key manager regeneration fails
- **THEN** 500 Internal Server Error with error details in response body
- **AND** Response contains error message and code 'master_key_regenerate_failed'

### Requirement: GETMasterKey

The API SHALL support `GET /admin/api/v1/developers/master-key` to retrieve the master api key. restricted from master api key authentication to prevent key exposure if the master key is leaked.

#### Scenario: MasterKeyBlockedFromRetrievingMasterKey
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** GET /admin/api/v1/developers/master-key is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedToRetrieveMasterKey
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** GET /admin/api/v1/developers/master-key is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: POSTMasterKey

The API SHALL support `POST /admin/api/v1/developers/master-key/regenerate` to regenerate the master api key. restricted from master api key authentication to prevent unauthorized key rotation if the master key is leaked.

#### Scenario: MasterKeyBlockedFromRegeneratingMasterKey
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/developers/master-key/regenerate is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedToRegenerateMasterKey
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/developers/master-key/regenerate is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: GETOptimization

The API SHALL support `GET /admin/optimization/compression` to returns current compression configuration including mode and lite settings

#### Scenario: RetrieveCompressionSettings
- **GIVEN** Admin user is authenticated
- **WHEN** GET /admin/optimization/compression is called
- **THEN** 200 OK with compression configuration JSON
- **AND** Response includes mode and lite configuration object

### Requirement: PUTOptimization

The API SHALL support `PUT /admin/optimization/compression` to persists compression configuration and flushes cache when settings change

#### Scenario: UpdateCompressionSettings
- **GIVEN** Admin user provides valid compression configuration
- **WHEN** PUT /admin/optimization/compression is called with mode and lite settings
- **THEN** 200 OK with updated compression configuration
- **AND** Cache is flushed to invalidate entries computed with previous configuration
- **AND** Returns updated compression settings

#### Scenario: InvalidCompressionSettings
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user provides invalid JSON payload
- **WHEN** PUT /admin/optimization/compression is called with malformed request
- **THEN** 400 Bad Request with error message
- **AND** Error details returned in response

### Requirement: GETOptimization

The API SHALL support `GET /admin/optimization/cache/stats` to returns current cache statistics including hits, misses, size and hit rate

#### Scenario: RetrieveCacheStatistics
- **GIVEN** Admin user is authenticated
- **WHEN** GET /admin/optimization/cache/stats is called
- **THEN** 200 OK with cache statistics JSON
- **AND** Response includes hits, misses, size and calculated hit_rate

### Requirement: POSTOptimization

The API SHALL support `POST /admin/optimization/cache/flush` to clears all cached responses from the cache storage

#### Scenario: FlushCache
- **GIVEN** Admin user is authenticated
- **WHEN** POST /admin/optimization/cache/flush is called
- **THEN** 200 OK with flushed confirmation
- **AND** All cache entries are removed
- **AND** Confirmation flag returned

### Requirement: GETOptimization

The API SHALL support `GET /admin/optimization/compression/metrics` to returns aggregate compression statistics per mode and totals from database

#### Scenario: RetrieveCompressionMetrics
- **GIVEN** Admin user is authenticated and metrics exist in database
- **WHEN** GET /admin/optimization/compression/metrics is called
- **THEN** 200 OK with compression metrics JSON
- **AND** Response includes per-mode breakdown with tokens_saved and savings_percent
- **AND** Totals calculated across all modes

#### Scenario: DatabaseErrorRetrievingMetrics
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Database query fails
- **WHEN** GET /admin/optimization/compression/metrics encounters database error
- **THEN** 500 Internal Server Error with error message
- **AND** Error details returned in response

### Requirement: POSTOptimization

The API SHALL support `POST /admin/optimization/compression/preview` to runs compression on a sample body and returns compression statistics without persisting

#### Scenario: PreviewCompressionOnSample
- **GIVEN** Admin user provides sample body text
- **WHEN** POST /admin/optimization/compression/preview is called with body and optional mode
- **THEN** 200 OK with compression preview results
- **AND** Uses current DB lite configuration when mode is lite
- **AND** Returns compressed text and token statistics

#### Scenario: MissingBodyInPreviewRequest
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user omits required body field
- **WHEN** POST /admin/optimization/compression/preview is called without body
- **THEN** 400 Bad Request with error message
- **AND** Error details returned in response

### Requirement: GETQuota

The API SHALL support `GET /api/admin/quota` to list quotas with filtering by provider, search term, and status, with pagination support.

#### Scenario: ListQuotasWithDefaultPagination
- **GIVEN** Admin user is authenticated
- **WHEN** GET /api/admin/quota is called with no query parameters
- **THEN** 200 OK with list of quotas
- **AND** Response includes paginated list of quotas

#### Scenario: ListQuotasWithProviderFilter
- **GIVEN** Admin user is authenticated and quotas exist for provider 'aws'
- **WHEN** GET /api/admin/quota?provider=aws is called
- **THEN** 200 OK with filtered quota list
- **AND** Only quotas matching filters are returned
- **AND** Pagination metadata is included

#### Scenario: ListQuotasWithCustomPagination
- **GIVEN** Admin user is authenticated and many quotas exist
- **WHEN** GET /api/admin/quota?page=2&per_page=25 is called
- **THEN** 200 OK with paginated results
- **AND** Results are limited to specified page size

### Requirement: GETQuota

The API SHALL support `GET /api/admin/quota/summary` to get summary of quota information including next reset dates and current month costs across all providers.

#### Scenario: GetQuotaSummary
- **GIVEN** Admin user is authenticated and quota data exists
- **WHEN** GET /api/admin/quota/summary is called
- **THEN** 200 OK with summary containing resets and spent amounts
- **AND** Summary includes next reset dates from quota.NextProviderResets
- **AND** Summary includes current month costs from usage.CostThisMonth
- **AND** Summary includes provider metadata from quota.ProviderMeta

#### Scenario: SummaryCalculatesEarliestResetCorrectly
- **GIVEN** Multiple providers with different reset schedules
- **WHEN** GET /api/admin/quota/summary is called with multiple providers
- **THEN** 200 OK with correct earliest reset date in summary
- **AND** earliestReset function parses reset dates with time.Parse
- **AND** Earliest reset date is determined using t.Before comparison

### Requirement: POSTQuota

The API SHALL support `POST /api/admin/quota/:connId/refresh` to refresh quota data for a specific connection by id.

#### Scenario: RefreshQuotaForValidConnection
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and connection exists
- **WHEN** POST /api/admin/quota/conn-123/refresh is called
- **THEN** 200 OK with refreshed quota data
- **AND** Quota data is refreshed from external provider

#### Scenario: RefreshQuotaForInvalidConnection
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but connection does not exist
- **WHEN** POST /api/admin/quota/invalid-id/refresh is called
- **THEN** 404 Not Found or error response
- **AND** Error is returned if connection not found

### Requirement: GETTLS

The API SHALL support `GET /api/admin/tls` to returns the current tls/https configuration including validity flag, certificate cache directory, and whether the https server is currently active.

#### Scenario: RetrieveCurrentTLSConfiguration
- **GIVEN** Admin user is authenticated and TLS configuration exists in data directory.
- **WHEN** GET /api/admin/tls is called.
- **THEN** 200 OK with TLS configuration details in response body.
- **AND** Response body contains data object with enabled, domain, email, acceptTOS, staging, certCache,
valid, certDir, and active fields.

#### Scenario: FailedToLoadTLSConfiguration
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but TLS configuration file is corrupted or unreadable.
- **WHEN** GET /api/admin/tls is called.
- **THEN** 500 Internal Server Error.
- **AND** Response body contains error message.

### Requirement: PUTTLS

The API SHALL support `PUT /api/admin/tls` to validates and persists a new tls/https configuration. if enabled, logs that a restart is required to activate https on port 443.

#### Scenario: UpdateTLSConfigurationSuccessfully
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and provides valid TLS configuration payload.
- **WHEN** PUT /api/admin/tls is called with valid JSON body.
- **THEN** 200 OK with ok: true in response body.
- **AND** If enabled, logs message indicating restart required to activate on :443.
- **AND** If certCache is empty in request, defaults to 'certs'.

#### Scenario: ValidationFailureOnTLSConfig
<!-- openlore-test: tags=smoke,regression (auto) -->
- **GIVEN** Admin user is authenticated but provides invalid TLS configuration (e.g., missing domain when
enabled).
- **WHEN** PUT /api/admin/tls is called with invalid configuration.
- **THEN** 400 Bad Request.
- **AND** Response body contains validation error message.

#### Scenario: MalformedRequestBody
- **GIVEN** Admin user is authenticated but sends malformed JSON or wrong types.
- **WHEN** PUT /api/admin/tls is called with malformed JSON.
- **THEN** 400 Bad Request.
- **AND** Response body contains error message from JSON binding.

#### Scenario: PersistFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but server fails to write configuration to disk.
- **WHEN** PUT /api/admin/tls is called and SaveHTTPSConfig fails.
- **THEN** 500 Internal Server Error.
- **AND** Response body contains error message from file system operation.

### Requirement: GETTLS

The API SHALL support `GET /api/admin/tls/public-ip` to returns the host's public ipv4 address as discovered via external service.

#### Scenario: RetrievePublicIPSuccessfully
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and external IP discovery service is reachable.
- **WHEN** GET /api/admin/tls/public-ip is called.
- **THEN** 200 OK with public IP address in response body.
- **AND** Response body contains data object with ip field.

#### Scenario: PublicIPDiscoveryFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but external IP discovery service is unreachable.
- **WHEN** GET /api/admin/tls/public-ip is called.
- **THEN** 502 Bad Gateway.
- **AND** Response body contains error message from network operation.

### Requirement: GETTLS

The API SHALL support `GET /api/admin/tls/check-dns` to resolves the requested domain and reports whether it points to the host's public ip address.

#### Scenario: CheckDNSResolutionMatchesPublicIP
- **GIVEN** Admin user is authenticated, provides domain query parameter, and DNS resolution succeeds.
- **WHEN** GET /api/admin/tls/check-dns?domain=example.com is called.
- **THEN** 200 OK with DNS check results in response body.
- **AND** Response body contains data object with domain, publicIP, resolvedIP, and matches fields.
- **AND** matches is true when resolvedIP equals publicIP and resolvedIP is not empty.

#### Scenario: MissingDomainParameter
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but omits domain query parameter.
- **WHEN** GET /api/admin/tls/check-dns is called without domain query parameter.
- **THEN** 400 Bad Request.
- **AND** Response body contains error message indicating domain parameter is required.

#### Scenario: PublicIPDiscoveryFailureDuringDNSCheck
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but external IP discovery service is unreachable.
- **WHEN** GET /api/admin/tls/check-dns?domain=example.com is called.
- **THEN** 502 Bad Gateway.
- **AND** Response body contains error message from network operation.
- **AND** resolvedIP will be empty string in successful response if DNS lookup fails.

### Requirement: POSTBackup

The API SHALL support `POST /api/admin/backup/download` to download a backup of the database as an ndjson stream.

#### Scenario: DownloadBackup
- **GIVEN** Authenticated admin user
- **WHEN** POST /api/admin/backup/download is called
- **THEN** 200 OK with NDJSON stream of backup data
- **AND** Response headers include Content-Type: application/x-ndjson
- **AND** Response headers include Content-Disposition: attachment; filename="axonrouter-backup.ndjson"
- **AND** Response body is a chunked NDJSON stream of backup data

### Requirement: POSTBackup

The API SHALL support `POST /api/admin/backup/restore` to restore the database from a backup file upload.

#### Scenario: RestoreFromBackup
- **GIVEN** Authenticated admin user with a valid backup file
- **WHEN** POST /api/admin/backup/restore is called with multipart form data containing backup file and
optional password
- **THEN** 200 OK with restore result data
- **AND** If restore is successful, the onRestoreRestart callback is invoked after a 2-second delay

#### Scenario: RestoreMissingFile
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated admin user without a backup file
- **WHEN** POST /api/admin/backup/restore is called without backup file
- **THEN** 400 Bad Request with error 'backup file is required'
- **AND** Error response contains specific error message

#### Scenario: RestoreInvalidFile
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated admin user with an invalid backup file
- **WHEN** POST /api/admin/backup/restore is called with corrupted or invalid backup file
- **THEN** 400 Bad Request with error details
- **AND** Error response contains specific error message

### Requirement: POSTBackup

The API SHALL support `POST /admin/api/v1/backup/download` to download a system backup. restricted from master api key authentication to prevent data exfiltration if the master key is leaked.

#### Scenario: MasterKeyBlockedFromBackupDownload
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/backup/download is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedForBackupDownload
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/backup/download is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: POSTBackup

The API SHALL support `POST /admin/api/v1/backup/restore` to restore a system backup. restricted from master api key authentication to prevent unauthorized data restoration if the master key is leaked.

#### Scenario: MasterKeyBlockedFromBackupRestore
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/backup/restore is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedForBackupRestore
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/backup/restore is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: GETRequestLog

The API SHALL support `GET /api/admin/logs` to returns paginated request logs with optional filters for provider, connection, model, combo, modality, status, date range, and search.

#### Scenario: ListLogsWithDefaultPagination
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs is called without query parameters
- **THEN** 200 OK with paginated list of request logs
- **AND** page defaults to 1
- **AND** per_page defaults to 100

#### Scenario: ListLogsWithFilters
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs?provider_id=xyz&start_date=2024-01-01 is called
- **THEN** 200 OK with filtered paginated list of request logs
- **AND** Filters applied: provider_id, connection_id, model_id, combo_id, modality, status, search,
start_date/end_date or since/until

### Requirement: DELETERequestLog

The API SHALL support `DELETE /api/admin/logs` to deletes request logs older than a specified retention window (7, 30, or 90 days).

#### Scenario: ClearOldLogsSuccessfully
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated admin user
- **WHEN** DELETE /api/admin/logs with body {"older_than_days": 30}
- **THEN** 200 OK with count of deleted rows
- **AND** older_than_days must be 7, 30, or 90

#### Scenario: ClearLogsWithInvalidRetentionDays
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated admin user
- **WHEN** DELETE /api/admin/logs with body {"older_than_days": 15}
- **THEN** 400 Bad Request with error message
- **AND** Validation error returned

### Requirement: GETRequestLog

The API SHALL support `GET /api/admin/logs/active` to returns currently in-flight proxied requests for the live in-flight panel.

#### Scenario: GetActiveRequests
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs/active is called
- **THEN** 200 OK with list of active/in-flight requests
- **AND** Data sourced from active request tracking system

### Requirement: GETRequestLog

The API SHALL support `GET /api/admin/logs/:id` to returns a single request log entry by id with full details including tokens, latency, cost, and metadata.

#### Scenario: GetExistingLog
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs/123 is called
- **THEN** 200 OK with log details
- **AND** Includes all token types, latency, status code, error message, cost, client IP, user agent

#### Scenario: GetNonExistentLog
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs/999 is called
- **THEN** 404 Not Found with error message
- **AND** Log ID does not exist in database

### Requirement: GETRequestLog

The API SHALL support `GET /api/admin/logs/stats` to returns aggregated log statistics including provider usage, model usage, and daily usage trends.

#### Scenario: GetStatsWithDefaultHours
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs/stats is called
- **THEN** 200 OK with provider_usage, model_usage, and daily_usage
- **AND** hours defaults to 24
- **AND** daily usage always returns 30 days

#### Scenario: GetStatsWithCustomHours
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/logs/stats?hours=48 is called
- **THEN** 200 OK with aggregated stats for specified hours
- **AND** Custom time window for provider and model usage

### Requirement: GETCombo

The API SHALL support `GET /api/admin/combos` to list returns all combos.

#### Scenario: ListAllCombos
- **GIVEN** Admin user is authenticated
- **WHEN** GET /api/admin/combos is called
- **THEN** 200 OK with list of combos
- **AND** Response contains array of all combo entities

### Requirement: GETCombo

The API SHALL support `GET /api/admin/combos/:id` to get returns a single combo with its steps.

#### Scenario: GetComboByID
- **GIVEN** Admin user is authenticated and combo exists
- **WHEN** GET /api/admin/combos/:id is called with valid ID
- **THEN** 200 OK with combo and steps
- **AND** Response includes combo details and associated steps

### Requirement: POSTCombo

The API SHALL support `POST /api/admin/combos` to create creates a new combo with steps.

#### Scenario: CreateNewCombo
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated with valid combo data
- **WHEN** POST /api/admin/combos is called
- **THEN** 201 Created with new combo
- **AND** Combo is persisted with associated steps
- **AND** Validation checks strategy and fusion config

### Requirement: PUTCombo

The API SHALL support `PUT /api/admin/combos/:id` to update modifies a combo.

#### Scenario: UpdateExistingCombo
- **GIVEN** Admin user is authenticated with valid update data
- **WHEN** PUT /api/admin/combos/:id is called
- **THEN** 200 OK with updated combo
- **AND** Validates strategy via isValidComboStrategy
- **AND** Parses and validates fusion config
- **AND** Normalizes strategy to lowercase trimmed

### Requirement: DELETECombo

The API SHALL support `DELETE /api/admin/combos/:id` to delete removes a combo.

#### Scenario: DeleteCombo
- **GIVEN** Admin user is authenticated and combo exists
- **WHEN** DELETE /api/admin/combos/:id is called
- **THEN** 204 No Content
- **AND** Associated steps may be cascade deleted

### Requirement: POSTCombo

The API SHALL support `POST /api/admin/combos/seed-defaults` to seeddefaults seeds default combos.

#### Scenario: SeedDefaultCombos
- **GIVEN** Admin user is authenticated
- **WHEN** POST /api/admin/combos/seed-defaults is called
- **THEN** 200 OK with seeding results
- **AND** Default combos are created if they don't exist
- **AND** Idempotent operation

### Requirement: POSTComboStep

The API SHALL support `POST /api/admin/combos/:id/steps` to addstep adds a step to a combo.

#### Scenario: AddStepToCombo
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated with valid step data
- **WHEN** POST /api/admin/combos/:id/steps is called
- **THEN** 201 Created with new step
- **AND** Step is appended to combo's step sequence

### Requirement: DELETEComboStep

The API SHALL support `DELETE /api/admin/combos/:id/steps/:stepId` to removestep removes a step from a combo.

#### Scenario: RemoveStepFromCombo
- **GIVEN** Admin user is authenticated and step exists
- **WHEN** DELETE /api/admin/combos/:id/steps/:stepId is called
- **THEN** 204 No Content
- **AND** Step is removed from combo's step sequence

### Requirement: GETComboMetric

The API SHALL support `GET /api/admin/combos/metrics` to query param `window` is the lookback window in seconds (default 86400, max 2592000).

#### Scenario: GetComboMetrics
- **GIVEN** Admin user is authenticated
- **WHEN** GET /api/admin/combos/metrics is called
- **THEN** 200 OK with aggregated metrics
- **AND** Default window is 86400 seconds (1 day)
- **AND** Maximum allowed window is 2592000 seconds (30 days)
- **AND** Metrics aggregated from request logs

### Requirement: GETSystemHealth

The API SHALL support `GET /health` to returns a simple liveness check for load balancers and dashboards. reports ok/degraded/error based on in-memory store and usage-tracker health, never blocks on db access. includes version info and password-change flag.

#### Scenario: HealthySystem
- **GIVEN** Application is running with valid store and tracker
- **WHEN** GET /health is called without authentication
- **THEN** 200 OK with status 'ok'
- **AND** Response body contains status, db, version, latest_version, update_available, must_change_password
fields

#### Scenario: DegradedSystem
- **GIVEN** Usage tracker has dropped events indicating write pipeline saturation
- **WHEN** GET /health is called without authentication
- **THEN** 200 OK with status 'degraded'
- **AND** Response body contains status 'degraded' and db 'degraded'

#### Scenario: UnhealthySystem
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Store or tracker is nil
- **WHEN** GET /health is called without authentication
- **THEN** 503 Service Unavailable with status 'error'
- **AND** Response body contains status 'error' and db 'error'

#### Scenario: PasswordChangeRequiredFlag
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Database has admin password settings
- **WHEN** GET /health is called
- **THEN** Response includes must_change_password boolean based on settings flags
- **AND** must_change_password is true when admin_password_changed flag is missing but password hash exists
- **AND** must_change_password is false when admin_password_changed is 'true'

### Requirement: GETVersion

The API SHALL support `GET /admin/check-update` to forces a fresh fetch of the latest github release and returns current version, latest version, and whether an update is available.

#### Scenario: SuccessfulUpdateCheck
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Version checker is initialized and GitHub API is reachable
- **WHEN** GET /admin/check-update is called by admin
- **THEN** 200 OK with version comparison info
- **AND** Response contains version, latest_version, update_available fields

#### Scenario: CheckerUnavailable
- **GIVEN** Version checker is nil
- **WHEN** GET /admin/check-update is called
- **THEN** 503 Service Unavailable
- **AND** Response contains error message

#### Scenario: GitHubAPIFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Version checker is initialized but GitHub API call fails
- **WHEN** GET /admin/check-update is called by admin
- **THEN** 502 Bad Gateway with error details
- **AND** Response contains error message from GitHub API

### Requirement: GETVersion

The API SHALL support `GET /admin/changelog` to returns the project's changelog markdown via the version checker. used by dashboard to avoid cors and ad-blocker issues from fetching raw.githubusercontent.com directly.

#### Scenario: SuccessfulChangelogRetrieval
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Version checker is initialized and changelog is available
- **WHEN** GET /admin/changelog is called by admin
- **THEN** 200 OK with markdown string
- **AND** Response contains markdown field with CHANGELOG content

#### Scenario: CheckerUnavailable
- **GIVEN** Version checker is nil
- **WHEN** GET /admin/changelog is called
- **THEN** 503 Service Unavailable
- **AND** Response contains error message

#### Scenario: ChangelogFetchFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Version checker is initialized but changelog fetch fails
- **WHEN** GET /admin/changelog is called by admin
- **THEN** 502 Bad Gateway with error details
- **AND** Response contains error message from changelog fetch

### Requirement: GETMetrics

The API SHALL support `GET /admin/metrics` to returns operational counters for observability including connection health, usage buffer state, rate-limited/quota-exhausted connection counts, and today's aggregated usage statistics.

#### Scenario: SuccessfulMetricsRetrieval
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Database is accessible and store/tracker are initialized
- **WHEN** GET /admin/metrics is called by admin
- **THEN** 200 OK with operational counters
- **AND** Response contains buffer_length, healthy_connections, dropped_usage_events, rate_limited,
quota_exhausted, requests_today, tokens_today, cost_today

#### Scenario: MetricsWithActiveConnections
- **GIVEN** Database has connection and usage data
- **WHEN** GET /admin/metrics is called by admin
- **THEN** 200 OK with accurate counts and aggregates
- **AND** rate_limited and quota_exhausted are counts of active connections with those statuses
- **AND** requests_today, tokens_today, cost_today are aggregated from usage data

### Requirement: GETDevice

The API SHALL support `GET /admin/devices/:id` to returns the key name and masked device list for the given api key.

#### Scenario: RetrieveDevicesForExistingAPIKey
- **GIVEN** Admin is authenticated and API key with ID exists
- **WHEN** GET /admin/devices/:id is called with valid key ID
- **THEN** 200 OK with key metadata and device list
- **AND** Response body contains keyId, name, count, and devices array

#### Scenario: APIKeyNotFound
- **GIVEN** Admin is authenticated and API key with ID does not exist
- **WHEN** GET /admin/devices/:id is called with invalid key ID
- **THEN** 404 Not Found
- **AND** Response body contains error message

#### Scenario: DatabaseError
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Database error occurs during query
- **WHEN** GET /admin/devices/:id is called
- **THEN** 500 Internal Server Error
- **AND** Response body contains error message

### Requirement: GETDashboard

The API SHALL support `GET /api/admin/dashboard/stats` to returns aggregated dashboard statistics including today summary and system metrics.

#### Scenario: RetrieveDashboardStats
- **GIVEN** Authenticated admin user with valid database connection and usage tracker
- **WHEN** GET /api/admin/dashboard/stats is called
- **THEN** 200 OK with JSON containing today summary and system metrics
- **AND** Response body contains today summary from usage aggregator
- **AND** Response body contains system metrics (CPU, memory, disk usage)

### Requirement: GETProvider

The API SHALL support `GET /api/admin/dashboard/provider-summary` to returns provider summary including disabled reasons so the dashboard can show why accounts are disabled.

#### Scenario: RetrieveProviderSummary
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/dashboard/provider-summary is called
- **THEN** 200 OK with JSON array of provider summaries including disabled reasons
- **AND** Response includes disabled_reasons field for each provider

### Requirement: GETLog

The API SHALL support `GET /api/admin/dashboard/recent-logs` to returns the last n log entries for the dashboard.

#### Scenario: RetrieveRecentLogs
- **GIVEN** Authenticated admin user
- **WHEN** GET /api/admin/dashboard/recent-logs is called with optional ?limit=N parameter
- **THEN** 200 OK with JSON array of last N log entries
- **AND** Default limit is applied if N not specified
- **AND** Logs are ordered by timestamp descending

### Requirement: POSTSystem

The API SHALL support `POST /api/admin/upgrade` to downloads the latest release binary, verifies its sha256 checksum, and replaces the current binary with the new version.

#### Scenario: SuccessfulUpgrade
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and version checker has latest release info
- **WHEN** POST /api/admin/upgrade is called
- **THEN** 200 OK with restart instructions in response body
- **AND** Response contains restart instructions with command and hint
- **AND** New binary is written to temporary path and atomically swapped

#### Scenario: UpgradeWithInvalidChecksum
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but downloaded binary has invalid checksum
- **WHEN** POST /api/admin/upgrade is called
- **THEN** 500 Internal Server Error
- **AND** Error message indicates checksum verification failed

#### Scenario: UpgradeDownloadFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but release download fails
- **WHEN** POST /api/admin/upgrade is called
- **THEN** 500 Internal Server Error
- **AND** Error message indicates download failure

### Requirement: POSTSystem

The API SHALL support `POST /api/admin/restart` to restarts the systemd service to apply a binary upgrade by delegating to a system-scoped unit.

#### Scenario: SuccessfulRestart
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and binary has been upgraded
- **WHEN** POST /api/admin/restart is called
- **THEN** 200 OK with restart initiated
- **AND** Service restart is triggered via system-scoped systemd unit

#### Scenario: RestartFailure
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated but systemd restart command fails
- **WHEN** POST /api/admin/restart is called
- **THEN** 500 Internal Server Error
- **AND** Error message indicates systemd command failure

### Requirement: POSTSystem

The API SHALL support `POST /admin/api/v1/upgrade` to trigger a system upgrade. restricted from master api key authentication to prevent unauthorized system modifications if the master key is leaked.

#### Scenario: MasterKeyBlockedFromSystemUpgrade
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/upgrade is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedForSystemUpgrade
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/upgrade is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: POSTSystem

The API SHALL support `POST /admin/api/v1/restart` to restart the system. restricted from master api key authentication to prevent unauthorized system restart if the master key is leaked.

#### Scenario: MasterKeyBlockedFromSystemRestart
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/restart is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedForSystemRestart
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/restart is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: GETConsoleLog

The API SHALL support `GET /admin/console-logs` to read the log file from the end and return up to maxconsolelines lines of the console log for admin monitoring.

#### Scenario: RetrieveConsoleLogsSuccessfully
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Admin user is authenticated and console log file exists at /tmp/axonrouter.log with content
- **WHEN** GET /admin/console-logs is called
- **THEN** 200 OK with JSON body containing up to 500 lines and the file path
- **AND** Response body contains Lines array and Path string

#### Scenario: ConsoleLogFileMissing
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated and console log file does not exist at /tmp/axonrouter.log
- **WHEN** GET /admin/console-logs is called
- **THEN** 200 OK with JSON body containing empty Lines array and the file path
- **AND** Response body contains empty Lines array and Path string

#### Scenario: ConsoleLogFileUnreadable
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Admin user is authenticated and console log file exists but is unreadable due to permissions
- **WHEN** GET /admin/console-logs is called
- **THEN** 500 Internal Server Error with JSON error body
- **AND** Response body contains error message

### Requirement: POSTOrder

The API SHALL support `POST /api/orders` to create a new order from the current cart.

#### Scenario: CreateOrder
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Authenticated user with valid cart
- **WHEN** POST /api/orders is called
- **THEN** 201 Created with orderId in response body
- **AND** 201 Created with orderId in response body

### Requirement: POSTAdmin

The API SHALL support `POST /admin/api/v1/change-password` to change the admin password. restricted from master api key authentication to prevent unauthorized password changes if the master key is leaked.

#### Scenario: MasterKeyBlockedFromPasswordChange
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/change-password is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedForPasswordChange
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/change-password is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: POSTAdmin

The API SHALL support `POST /admin/api/v1/defer-password-change` to defer a required password change. restricted from master api key authentication to prevent bypassing security policies if the master key is leaked.

#### Scenario: MasterKeyBlockedFromDeferringPasswordChange
<!-- openlore-test: priority=high tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** POST /admin/api/v1/defer-password-change is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedToDeferPasswordChange
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** POST /admin/api/v1/defer-password-change is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler

### Requirement: PUTTLSConfig

The API SHALL support `PUT /admin/api/v1/tls-config` to update tls configuration. restricted from master api key authentication to prevent unauthorized security configuration changes if the master key is leaked.

#### Scenario: MasterKeyBlockedFromTLSConfigUpdate
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Request is authenticated with master API key
- **WHEN** PUT /admin/api/v1/tls-config is called with master key auth
- **THEN** Request is rejected with 403 Forbidden
- **AND** Response contains error code 'master_action_forbidden'
- **AND** HTTP status is 403 Forbidden

#### Scenario: SessionAuthAllowedForTLSConfigUpdate
<!-- openlore-test: priority=high (auto) -->
- **GIVEN** Request is authenticated with session-based dashboard auth
- **WHEN** PUT /admin/api/v1/tls-config is called with session auth
- **THEN** Request is allowed to proceed
- **AND** Request proceeds to handler
